repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.16.4
    hooks:
      - id: ruff-check
        files: ^backend/
        args: [--fix]

      - id: ruff-format
        files: ^backend/